export interface SubscriptionPlan {
  id: string;
  name: string;
  slug: string;
  description: string;
  priceCents: number;
  annualPriceCents?: number | null;
  features: string[];
  sortOrder: number;
}
export interface SubscriptionPlan {
  id: string;
  name: string;
  slug: string;
  description: string;
  priceCents: number;
  annualPriceCents?: number | null;
  features: string[];
  sortOrder: number;
}

